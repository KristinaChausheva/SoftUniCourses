function soleve(params) {
    let num = Number(params.shift());

    if ((num >= - 100 && num <= 100) && (num != 0)) {
        console.log("Yes");
        
    } else {
        console.log("No");
        
    }
}