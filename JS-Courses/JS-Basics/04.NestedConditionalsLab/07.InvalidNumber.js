function solve(params) {
    const num = Number(params.shift());
    if ((num >= 100 && num <= 200) || num == 0) {
        
    } 
    else {
        console.log("invalid");
        
    }
}

solve([0])